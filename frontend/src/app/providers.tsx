// frontend/src/app/providers.tsx
'use client'

/**
 * Providers component simplified - authentication removed.
 * Add your own providers here as needed.
 */
export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <>
      {children}
    </>
  )
}
